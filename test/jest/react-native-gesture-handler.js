const chain = {
  activateAfterLongPress: () => chain,
  enabled: () => chain,
  onFinalize: () => chain,
  onStart: () => chain,
  onUpdate: () => chain,
};

module.exports = {
  Gesture: {
    Pan: () => chain,
  },
  GestureDetector: 'GestureDetector',
};
