/**
 * Native stub — the breadth landing is web-only by definition: app/index.tsx
 * redirects native (and signed-in) visitors before this can render. The real
 * implementation is BreadthLandingPage.web.tsx (verbatim-injected mock).
 */

export function BreadthLandingPage() {
  return null;
}

export default BreadthLandingPage;
